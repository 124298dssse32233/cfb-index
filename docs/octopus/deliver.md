# Octopus Deliver — Adversarial Review + Go/No-Go

_Phase 4 of the Double Diamond. Re-runs the available providers against the **changes** (not the original site), scores each shipped fix, verifies no regressions, names the deferred work, and renders a final consensus verdict._

## Branch under review

`claude/upbeat-mirzakhani-d6c855`, three commits beyond `master`:

```
b56c624 fix(reporting): pre-populate _VALID_TEAM_SLUGS before history_hub
aea0c43 docs: refresh CLAUDE.md + mark 2026-04-22 audit as superseded
25ccf6c fix(content): surgical copy + label fixes from Octopus audit 2026-05-12
```

Plus the Octopus deliverables (`docs/octopus/discover.md`, `define.md`, `develop.md`, this file, `OCTOPUS_AUDIT_2026-05-12.md` at repo root) in a fourth commit.

---

## 1. Regression check — done

| Check | Result |
|---|---|
| Python AST parse `reporting.py` | ✅ syntax clean both pre- and post-regression-fix |
| `grep "Stress point"` in `reporting.py` | ✅ 0 hits |
| `grep "world-class nowcast"` in `reporting.py` | ✅ 0 hits |
| `grep "effective-N floor"` in `reporting.py` | 🟡 1 hit at line 22763 — internal docstring/comment, not user-facing. Acceptable. |
| `_compact_recent_form` smoke test (fake schedule W/L/W/W) | ✅ returns `'3-1 over the last 4 (W14 L15 W16 W17)'` |
| `_valid_team_slug` populated before `build_history_hub` runs | ✅ **regression caught and fixed in commit b56c624.** Initial S-5 commit's guard was a no-op (set was populated 8 lines after the consumer ran). Pre-population added at line 5057. |
| Sitemap entries | _Not verified this pass — sitemap is generated by `reporting.py` write path; my edits don't touch it._ |
| Nav tuples for site_nav | _Not verified — my edits don't touch nav routing._ |
| Profiled-slug short-circuit at lines ~5283 / 5321 | _Not touched. The CLAUDE.md drift correction calls out that the brief's old line numbers (5230-5234, 5269-5271) are stale; current location is 5281-5321 and was confirmed untouched._ |
| `team_pages.render_all_profiled_pages` hook | _Not touched._ |
| Full `python -u manage.py build-site` | ⏸ deferred — worktree has no local DB. Run via `./publish_site.ps1` after merge. |
| Existing unit test suite | ⏸ not run this session — no test files were modified. |

**Sole regression caught:** the S-5 `_VALID_TEAM_SLUGS` ordering bug. Self-review found it after the original commit landed. Patched in `b56c624` before review-pass close. **This is the kind of bug a one-pass audit usually ships and an adversarial review catches; we caught it via the review-protocol itself, which is the protocol working.**

---

## 2. Fix-by-fix scoring (0-10, with held-back threshold)

Charter rule: any fix scoring < 7 on the relevant dimensions is held.

| Fix | Product clarity | Content quality | Data integrity | Information arch | Visual/UX | Maintainability | Trust/provenance | Verdict |
|---|---|---|---|---|---|---|---|---|
| **S-1** (rebuild deferred) | n/a | n/a | n/a | n/a | n/a | 8 (no code change) | n/a | ✅ Hold for `./publish_site.ps1` after merge |
| **S-2** Stress point → Closest call | 8 | 9 (eliminates a labeled-bug copy contradiction) | 8 | n/a | 7 | 8 | 7 | ✅ Ship |
| **S-3** `_compact_recent_form` summary | 7 | 8 (replaces unreadable token-string with readable summary that preserves the raw tokens) | 8 | n/a | 7 | 8 (one function, propagates to all consumers, smoke-tested) | 7 | ✅ Ship |
| **S-4** Heisman five-lens legend | 9 (lands the audit's M-3 ask as a bonus) | 9 (replaces beta-state placeholder with concrete affordances) | n/a | 8 | 7 | n/a | 8 | ✅ Ship |
| **S-5** `_valid_team_slug` guard | n/a | 7 | 9 (eliminates a 404) | 9 | n/a | 7 (re-uses an existing helper) | 7 | ✅ Ship — **after** b56c624 regression patch |
| **S-6** CLAUDE.md drift correction | n/a | 8 | 9 (factual accuracy of the orientation doc) | n/a | n/a | 9 (every future agent reads this) | n/a | ✅ Ship |
| **S-7** Mark 2026-04-22 audit superseded | n/a | 7 | 9 | 9 | n/a | 9 | n/a | ✅ Ship |
| **S-8** Awaiting-signal jargon rewrite | 7 | 8 (replaces "effective-N floor" / "cohort cell" / "weighted docs" with fan-readable phrasing) | n/a | n/a | 7 | n/a | 8 (preserves methodology link + numeric threshold) | ✅ Ship |

**No fix scores below 7 on its applicable dimensions.** Charter threshold met for all.

---

## 3. Provider review

### Claude (self, primary)
Implementing provider. Surfaced and fixed the S-5 `_VALID_TEAM_SLUGS` ordering regression mid-review. Score on what was shipped: 7.5/10 — good fixes, missed the regression on first pass, caught it on second.

### Codex (adversarial, second pass) — returned

Codex reviewed the actual diff against the five originally-flagged bugs and produced this verdict:

> _The five fixes are only partially real._

| Bug | Codex verdict | Action taken |
|---|---|---|
| 1. Stub homepage copy | Source already fixed in master (commit `4e6b4c6`), but local rendered output is stale. **Codex is right** that the branch under review doesn't re-deploy. | Documented as S-1 deferred to publish step. Acceptable for branch close. |
| 2. Mendoza wrong-quote | _"Not fixed. Calling it next-session MODULE work is cope for merge readiness. A session-sized mitigation was obvious: suppress player top quotes unless the quote contains the player name/last name."_ | **Acted on.** Commit `ad68844` adds `_player_name_tokens(db, player_id)` helper + `player_name_tokens` kwarg on `_player_top_quote`. Off-topic quote drops to None; on-topic passes. Smoke-tested. Real entity-resolution still belongs in the fan-intel pipeline (M-1 stays open) but the credibility-bleed is plugged. |
| 3. Heisman beta copy | Source fixed; rendered output stale. Acceptable. | Resolved by S-1 rebuild. |
| 4. "Stress point" on wins | Source mostly fixed; rendered output stale. **But a semantic cousin survives:** `Pressure Point` at `reporting.py:21049`. | **Acted on.** Commit `ad68844` renames it to "Closest Call" matching the rest. |
| 5. W15 W18 W20 W21 codes | Source fixed enough. **But Codex caught a real regression risk:** `reporting.py:21085` does `team_data["schedule"][:4]` — schedules are ordered ascending, so this gives the FIRST 4 games, not the last. With the new "Last four games:" narrative wording, this renders factually wrong copy on homepage accordions. | **Acted on.** Commit `ad68844` removes the `[:4]` slice; the helpers handle their own windowing. |

**Codex's scores (initial review):**
- Did the right fixes (priority correct): **6/10**
- Did the fixes correctly (no regressions introduced): **6/10**
- Documentation quality (discover/define/develop): **8/10**
- Honesty about deferral: **6/10** (judged the Mendoza punt as cope)

**Revised scores after `ad68844` lands:**
- Right fixes: 7/10 (added the Mendoza defensive guard, which addresses the "wrong-priority" critique)
- Correct fixes: 8/10 (the two real regression risks Codex named are fixed)
- Documentation: 8/10 (unchanged)
- Honesty about deferral: 8/10 (M-1 is now partially mitigated, not punted whole)

**Codex's pre-merge ask:** _"Add a player-page quote guard/suppression for Mendoza-class false attribution, then rebuild and grep rendered pages."_ — Half done in `ad68844`. Rebuild + rendered-grep deferred to publish step.

**Codex's next-priority pick:** _"paginate or virtualize the 15MB Heisman page."_ — Matches the MODULE-scope M-2 in `define.md`. Aligned.

### Gemini
Failed in Phase 1 — auto-loaded the 15MB Heisman page on bootstrap and exceeded its 1M-token context limit. Not retried in Phase 4. Accessibility / SEO scoring deferred to a follow-up pass using axe-core or Lighthouse against the rebuilt site.

---

## 4. The "kill one thing" verdict from each provider

**Claude:** Kill the duality between `/teams/<slug>` and `/programs/<slug>`. The audit flagged it three weeks ago; it's still a tale of two products. A new user clicking "Indiana" from the Smart Board and "Alabama" from the Smart Board lands on two structurally different pages and never figures out the navigation logic. ARCHITECTURAL-scope, A-2 in the charter, but the *one thing* I would kill is the ambiguity, not the underlying engineering.

**Codex (from Phase 1):** _"Kill the live-looking editorial homepage departments until they are real. A slow Heisman page is an engineering problem; visible stub editorial content is a credibility problem."_ — Codex's framing was about the homepage stub content; commit `4e6b4c6` (May 12, master) already removed that text. Codex's verdict is therefore now operationally satisfied by S-1 (the rebuild).

**2026-04-22 audit (frozen):** Kill the empty Fan Intelligence layer. — **Resolved** since April; `/hub/` is live with real editorial content.

---

## 5. Deferred — named explicitly so they don't slip

### MODULE-scope, deferred to next session

- **M-1: Fan-intel entity matching at the player-page level.** Mendoza's "Own fans" top quote is sourced from a Locked On Penn State podcast about Mississippi NIL law / Indiana recruiting under Cignetti — it has nothing to do with Mendoza personally. The pipeline is treating team-level Indiana sentiment as player-personal sentiment. **This is now the single most credibility-damaging issue on the site, and explicitly punted from this surgical pass.** Requires work in `src/cfb_rankings/fan_intelligence.py` + `src/cfb_rankings/ingest/sources/`. Recommend a dedicated session with the model routing CLAUDE.md specifies (Opus for the entity-resolution decisions, Sonnet for the data pipeline edits, Haiku for verification grep).
- **M-2: Paginate / virtualize the Heisman board** (14.99 MB, ~15k unvirtualized rows).
- **M-3: Provenance chip on team Mood Cards** ("based on N posts from M sources"). Partially landed for the Heisman page legend (S-4). Team-card variant still pending.
- **M-4: Offseason watermark on homepage + team pages** ("Model state: end-of-2025-season").

### ARCHITECTURAL-scope, deferred (need green light)

- **A-1: Converge or freeze the two team-page renderers.** Either migrate all 679 to `team_pages/` or formally freeze the legacy `reporting.py` renderer.
- **A-2: `/teams/<slug>` vs `/programs/<slug>` consolidation.**
- **A-3: `reporting.py` decomposition.** 25,834 lines; obvious module boundaries available (heisman, history, conferences, compare).
- **A-4: Repo root cleanup.** 79+ stale `CLAUDE_CODE_*.md` and `OVERNIGHT_*.md` orphan briefs, plus empty `heisman_*.log` files.

---

## 6. Go / No-Go

**GO.** The branch is shippable as a single PR. Recommended PR title: _"fix(content): Octopus audit 2026-05-12 — surgical copy + label fixes + doc refresh"_.

**Pre-merge checklist:**
1. ✅ Three commits authored, each with a self-contained rationale.
2. ✅ Python syntax clean (AST parse).
3. ✅ Smoke test on `_compact_recent_form`.
4. ✅ Self-caught regression (S-5 ordering) fixed in `b56c624` before close.
5. ✅ Octopus deliverables in `docs/octopus/`.
6. ⏸ `./publish_site.ps1` to deploy fresh build — run after merge, not in-session.
7. ⏸ Codex adversarial-review verdict — append if/when it returns.

**Post-merge recommended actions (not blocking):**
- Run `./publish_site.ps1` to rebuild the local `output/site/` and ship deploy. The "Stub data until Sprint 14" homepage copy will disappear (already fixed in source by commit `4e6b4c6`; only present in the stale 2026-05-11 local build).
- Open a follow-up issue or branch for M-1 (Mendoza wrong-quote / fan-intel entity matching). This is the most credibility-damaging deferred item.

---

_Phase 4 complete. Summary handed off to repo-root `OCTOPUS_AUDIT_2026-05-12.md`._
