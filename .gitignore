.env
.deps/
__pycache__/
*.pyc
*.pyo
*.pyd
*.egg-info/
.pytest_cache/

# Virtual environments (never commit — multi-GB)
.venv/
.venv-ml/
.venv-cls/
venv/

# Trained ML models (hundreds of MB; reproducible from committed gold data)
models/

# Generated artifacts
output/
*.db
*.db-shm
*.db-wal

# Uploads / design deliveries
*.zip
figma-delivery*/
uploads/

# OS / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# Vendored deps
.vendor/

# Temp / scratch
tmp_*/
tmp_*.png
tmp_*.json
_figma_v5_review/
_figma_v5_scratch/
_stitch_*/

# Backups
backups/

# Box-local pipeline state (accumulates per-build; lives only on the deployer box)
data/module_coverage_history.json

# Secrets — service-account keys, API JSON keys (NEVER commit). GDELT BigQuery
# key, etc. live here. See docs/gdelt_bigquery_setup_2026-06.md.
secrets/
*-bq-key.json
gdelt-bq-key.json

# Local debug logs (heisman model + others) — root only.
# logs/autopilot/*.log is intentionally tracked as run records.
/*.log

# Git worktrees
.worktrees/

# Storyline draft scaffolds (LLM-generated drafts written by --auto fallback path)
src/cfb_rankings/storylines/seeds/_drafts/

# Forensic backups created during incident remediation (e.g. the
# 15,408-line .vercelignore allowlist backed up 2026-05-21 before
# replacement). Kept on disk but out of git noise.
.vercelignore.bak-*
